events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        server_name i11e201.p.ssafy.io www.i11e201.p.ssafy.io;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl;
        server_name i11e201.p.ssafy.io www.i11e201.p.ssafy.io;

        ssl_certificate /etc/letsencrypt/live/i11e201.p.ssafy.io/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/i11e201.p.ssafy.io/privkey.pem;

        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_prefer_server_ciphers on;
        ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-SHA384;

        location /jenkins {
            proxy_pass http://jenkins:8080;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        location /api {
            proxy_pass http://spring-app:8081;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        # Uncomment and adjust if you want to use the React app
        # location / {
        #     proxy_pass http://react-app:3000;
        #     proxy_set_header Host $host;
        #     proxy_set_header X-Real-IP $remote_addr;
        # }
    }
}